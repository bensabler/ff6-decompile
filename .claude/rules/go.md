---
paths:
  - "**/*.go"
---

Use idiomatic Go. Keep command wiring out of domain packages. Use explicit fixed-width arithmetic for SNES formats. Bounds-check every binary read. Wrap errors with context. Avoid global mutable state. Add tests. Add fuzzing for binary decoders.
