Use the tileset-reconstructor, background-reconstructor, palette-researcher, struct-recovery, and dma-tracer skills. Follow `.claude/playbooks/RECOVER_MAP.md`.

Target: $ARGUMENTS

Record the map header record and its fields, tileset and tilemap sources, palette selection, collision data, exits, event-trigger placement, layer order and priority, scroll behavior, and the per-map values for at least two maps so a field is distinguished from a constant.
