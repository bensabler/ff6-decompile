Alias for `/reconstruct-background`. The playbook is named `RECOVER_BACKGROUND.md`, so both spellings resolve here rather than one of them silently not existing.

Use the background-reconstructor, tileset-reconstructor, palette-researcher, dma-tracer, and asset-validator skills. Follow `.claude/playbooks/RECOVER_BACKGROUND.md`.

Target: $ARGUMENTS
