Alias for `/reconstruct-tileset`. The playbook is named `RECOVER_TILESET.md`, so both spellings resolve here rather than one of them silently not existing.

Use the tileset-reconstructor, palette-researcher, dma-tracer, asset-validator, and go-architect skills. Follow `.claude/playbooks/RECOVER_TILESET.md`.

Target: $ARGUMENTS
