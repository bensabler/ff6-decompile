Alias for `/reconstruct-sprite`. The playbook is named `RECOVER_SPRITE.md`, so both spellings resolve here rather than one of them silently not existing.

Use the sprite-reconstructor, graphics-archaeologist, dma-tracer, asset-validator, and go-architect skills. Follow `.claude/playbooks/RECOVER_SPRITE.md`.

Target: $ARGUMENTS

Recover tiles, palette, OAM/layout, origin, offsets, flips, sizes, priority, animation relationship, source provenance, local preview, manifest, and pixel validation.
