Use the context-manager, quality-auditor, go-architect, and documentation-curator skills.

Perform the Version 4 installation/migration workflow:

1. inventory all existing files and research;
2. preserve prior canonical records;
3. identify duplicate or conflicting command/skill files;
4. record OS, Git, Go, Claude Code, and exact Mesen version;
5. calculate the local ROM SHA-256 without copying it;
6. verify `local_artifacts/` and `.gitignore`;
7. initialize missing dashboards, indexes, and manifests;
8. run Go quality gates;
9. write `docs/migrations/V4_MIGRATION_REPORT.md`;
10. create a restart-safe checkpoint.

Do not begin a new reverse-engineering experiment.
