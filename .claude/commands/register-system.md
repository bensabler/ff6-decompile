Use the census-observer skill.

Register one named system in the content census: `$ARGUMENTS`
describes the system. Create the entry at an honest status
(UNMAPPED/OBSERVED/CANDIDATE_LOCATION unless existing evidence
supports more), cite evidence, list unknowns, set one bounded
next_action, and run `ff6lab census sync`.
