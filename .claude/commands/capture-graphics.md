Use the graphics-archaeologist, mesen-operator, experiment-designer, and asset-cataloger skills. Follow `.claude/playbooks/CAPTURE_GRAPHICS.md`.

Target: $ARGUMENTS

Create a deterministic runtime capture set with frame identity, PPU state, VRAM, CGRAM, OAM, layers, viewer captures, memory hashes, and provenance status.
