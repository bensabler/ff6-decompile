Use the census-observer skill.

Review the most recent experiment's evidence for newly observed
systems, register them in `manifests/content-census.json` (and
`manifests/rom-regions.json` where ROM knowledge emerged), run
`ff6lab census sync`, and return to the active research question
without expanding its scope.
