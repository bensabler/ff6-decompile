Use the census-observer skill.

Reconcile the census with recent work: update statuses that new
experiments/discoveries/implementations justify (cite the evidence),
link related records, run `ff6lab census sync`, and report
`ff6lab coverage summary` plus the top items from
`ff6lab coverage gaps`.
