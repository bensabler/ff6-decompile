# Checkpoint 2026-07-30 — EXP-0023/GFX-0001: graphics vertical proof (Unit 4)

## Current question
None open in this unit. Next: Unit 5 audio vertical (cursor/confirm
SFX), then final synchronization.

## State
Headless Mesen live (bridge v2 + extended read memtypes
vram/cgram/oam/aram/rom/dsp; probes EXP-0023 armed). Evidence frozen
at `local_artifacts/experiments/EXP-0023/` (hashes written). Battle
savestate intact.

## Work completed
EXP-0023 + GFX-0001: battle HUD font vertical proof — atomic capture
at anchor+120 (mode 1, BG3 chr word $5000, tilemap $7800, palette 0);
ROM search of 15 distinctive tiles → unanimous raw-copy base
ROMFILE 0x046FC0; identical run tiles $FF-$1FF containing all 37
HUD-referenced tiles; Go `internal/graphics/tile2bpp` (tests + fuzz) +
`ff6lab tiles decode2bpp`; decoded VRAM == decoded ROM on sampled
glyphs. M2 complete, M6 in progress. First graphics asset record
(GFX-0001) and first GRAPHICS_ASSETS index entry.

## Tests and quality gates
Run at commit: gofmt clean, build/vet pass, `go test ./...`
(9 test-bearing packages), audit clean.

## Git status
`main`, 9 ahead of origin after this commit. Not pushed.

## Blockers
None hard. Soft: load-path (ROM→VRAM) trace queued; glyph semantics
unassigned; MP savestate; GUI/testrunner parity.

## Exact next action
Unit 5 (audio vertical): plan EXP-0024 — from the battle savestate,
trigger the menu cursor/confirm SFX with a frame-scheduled A press
while watching CPU→APU port writes ($2140-$2143); capture the APU
command bytes, then dump ARAM (aram memtype now available) around the
driver state to locate the SFX dispatch; record trigger→port→driver
evidence chain with hashes (sequence/sample decode may bound out to a
follow-up if the driver protocol is deep).
