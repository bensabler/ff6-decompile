# Checkpoint 2026-07-31 — EXP-0028: monster database located (Unit 28)

## Current question
None open in this unit. Next selection: the populate routine's
X-computation (formation→monster-id mapping), which unlocks
formations and encounter packs together.

## State
Lab shut down. Evidence frozen at
`local_artifacts/experiments/EXP-0028/` (4 files, hashes.sha256).
Census: 47 entries; ROM ledger 23 regions (monster table + populate
routine added).

## Work completed
EXP-0028: mines encounter walk triggered battle init under a
write-watch; the enemy populate routine ($C22CA3-$C22D8C) reads
32-byte monster records at `ROMCPU:$CF0000` straight into the
Confirmed battle arrays (13 field offsets mapped: +$01 power,
+$08 HP, +$0A MP, staging pairs). Stride 32 and the field map
Confirmed by the records-77/78 powers 19/13 cross-check against
EXP-0018's live captures. EXP-0004 question 19b closed ($C22CCE =
max-HP store). Aux bank-$CF tables and random-encounter triggering
registered. Inventories updated (monsters.json holds the two
anchored records).

## Tests and quality gates
Run at commit: gofmt clean, build/vet pass, `go test ./...`
(10 packages), `ff6lab audit` clean.

## Git status
`main`, 2 ahead of origin after this commit. Not pushed.

## Blockers
None hard. Soft items unchanged.

## Exact next action
EXP-0029: dump the code upstream of `$C22CA3` (the caller that loads
X=$09A0) and decode the monster-id→record-offset computation plus its
input — the formation record consumer. Expected: a formation
structure naming monster ids for the encounter; register the
formation table region and update CEN-MONSTER-0002.
