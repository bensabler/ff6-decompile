# Checkpoint 2026-07-30 — EXP-0020 (candidate refuted) + bridge v2 live

## Current question
#30, refined: what drives action selection/scheduling divergence, and is
any RNG consumed at all? → EXP-0021.

## State
Mesen running on **bridge v2** with the tracked probe `EXP-0020` armed
(globals `exp20_reads/mvn/pop`, `+$3A70` writer census). Trial asset
`mesen/out/exp10-battle.mss` intact. Evidence archived immutably at
`local_artifacts/experiments/EXP-0020/` (events.log, commands.log,
experiment.json, hashes.sha256) per the new layout.

## Work completed
EXP-0020: `+$3A70` **refuted** as the timing-varying state (identical
matched-ordinal reads `$01`/`$00`); writer set identified
(`$C21629`/`$C2328C`/`$C22640`, write-then-read within an action);
divergence localized to action count/timing (2 vs 6 in matched windows)
→ scheduling interpretation (Tentative). Bridge v2 validated live;
three defects found and fixed during validation.

## Tests and quality gates
gofmt clean; build/vet pass; `go test ./...` pass (7 packages);
`go build ./cmd/ff6lab` pass; `ff6lab audit` clean.

## Git status
`main`; committing this unit now (6 ahead of origin). Not pushed.

## Blockers
None hard.

## Exact next action
EXP-0021: extend the probe to log the attack-record index at
`ROMCPU:$C22966` entry (A/X) plus scheduler-adjacent reads in the
`$C23190` region; run identical-state trials A/B; if index sequences
differ under matched inputs, the selection input is upstream scheduler
state — then watch that state's writers for a draw.

## Then (operator brief Units 4–5, not yet started)
Graphics vertical proof (menu font / HUD tiles: runtime → DMA → ROM →
decoder → comparison) and audio vertical proof (cursor SFX: trigger →
APU → sequence/sample → decode). Both are greenfield; the battle domain
is at a natural pause point.
