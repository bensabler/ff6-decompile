# Testing Strategy

Use table tests for format behavior, synthetic fixtures, golden outputs for deterministic renderers, fuzz tests for binary decoders, property tests where useful, integration tests for CLI workflows, and benchmarks only for demonstrated hotspots.
